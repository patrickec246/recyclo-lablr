Client:
- Update point sorting method to prevent intesections on polygons
    - Enforce point sorting after all relevant point operations
- Redesign shapeDock to be smaller
- Visual touchups to sidebar
    - Reorganize buttons
- Drawing animations during initial polygon creation


Server:
- Hookup Flask server (this code is already done but on home computer, save for last)
- Finish data pipeline processor (vid -> frame -> annotation -> reframe) (also on home cpu)
- Calculate average/last frame annotations for pre-rendering
- Peek last/next image(s)
